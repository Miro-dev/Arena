const Hit = {
  force: 20, // stance, quickness, stamina, mastery
  accuracy: 45, // stance, quickness, stamina, mastery
  vulnaribilityFound: 22 // mastery
};

const Kick = {
  force: 20, // stance, quickness, stamina, mastery
  accuracy: 45, // stance, quickness, stamina, mastery
  vulnaribilityFound: 22 // mastery
};

const Faint = {
  force: 20, // stance, quickness, stamina, mastery
  accuracy: 45, // stance, quickness, stamina, mastery
  vulnaribilityFound: 22 // mastery
};

const Rest = {
  force: 20, // stance, quickness, stamina, mastery
  accuracy: 45, // stance, quickness, stamina, mastery
  vulnaribilityFound: 22 // mastery
};

const Taunt = {
  force: 20, // stance, quickness, stamina, mastery
  accuracy: 45, // stance, quickness, stamina, mastery
  vulnaribilityFound: 22 // mastery
};

module.exports = Attack;
